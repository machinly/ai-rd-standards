# 研发工作约定

## 适用范围

本约定仅在用户明确调用 `$opc-rd` 或明确要求使用本规范时启用。没有明确启用时，按当前任务本身最直接、最合适的方式完成工作。

## 1. 默认直接执行

默认直接完成请求，不因为工作涉及代码、多个文件、AI、界面、耗时或跨会话，就自动增加规格、计划、状态文件、评审、Agent 或流程工具。

只有当某个工件真实帮助锁定长期契约、恢复上下文、管理重要风险或完成明确验收时，才创建它。

## 2. 需要长期契约时使用一个 OpenSpec change

满足以下任一条件时，可以创建或继续一个 OpenSpec change：

- 改变需要长期维护的产品行为、API、数据结构、AI 行为、基础设施或发布契约；
- 工作必须跨会话恢复；
- 存在多个重要验收条件或关键技术取舍；
- 失败影响较大，需要明确停止条件和恢复方式。

跨文件、用户可见或使用 AI 本身，不构成必须使用 OpenSpec 的理由。

使用 OpenSpec 时，`tasks.md` 是该变更唯一的执行状态；不得再维护内容重复的计划或状态文件。

## 3. 人保留关键决定

用户始终决定产品方向、用户价值、风险接受、供应商与数据用途、公开承诺以及真实高影响副作用。

流程、测试、评审、OpenSpec 或 AI 推断都不能代替这些决定。

## 4. 高影响动作需要当次明确授权

在执行以下真实动作前，必须说明具体目标、预期影响、验证方式和可行恢复路径，并取得用户对该次动作的明确授权：

- 生产环境变更；
- 客户或敏感数据处理；
- 凭据、权限或身份变化；
- 付款、退款或资金动作；
- 对外通信或公开承诺；
- 删除、覆盖或其他难以恢复的操作；
- 其他不可逆副作用。

准备、分析、本地实现、测试和演练不自动获得执行真实副作用的权限。

## 5. 保持范围并保护已有工作

不得静默扩大产品、风险或发布范围。不得覆盖、删除或重写与请求无关的用户工作。

实现中发现上游前提不成立时，应说明发现和影响，再回到拥有该决定的人，而不是在下游自行改写目标。

## 6. 按风险验证

运行能够真实支持结论的最少检查。检查强度随失败影响、可逆性和变更范围提高。

必须准确区分单元测试、集成测试、浏览器旅程、供应商 sandbox、生产观察和人工检查；局部证据不得支持更高层级的完成声明。

## 7. 按风险审查

所有工作由执行者自检。

只有涉及实质安全、权限、客户数据、资金、不可逆行为，或用户明确要求时，才要求未参与产出的独立审查。普通跨文件或用户可见变更不自动触发独立终审。

## 8. 一个事实只有一个当前位置

同一任务只维护一个当前状态来源。历史记录、旧评审和归档不得覆盖较新的失败、撤销或变更请求。

规范、项目文档、OpenSpec 和工具不得复制同一事实形成竞争入口。

## 9. 技术栈偏好不是通用流程规则

Go、Kratos、MySQL、sqlc、Vite、React、目录布局、表名、测试命令和 UI 风格属于项目模板、技术栈偏好或项目本地约定，不属于通用研发流程。

项目或用户已作出的选择优先；偏离个人技术偏好不需要额外治理工件。

## 10. 完成报告

结束时只报告：

- 实际改变了什么；
- 实际运行了哪些检查及结果；
- 尚存的实质风险或未完成事项。
