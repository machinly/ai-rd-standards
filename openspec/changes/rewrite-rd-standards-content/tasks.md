## 1. Freeze content execution

- [x] 1.1 Strict validate Change B and confirm G1 / `tooling_ready` is the last trustworthy state.
- [x] 1.2 Create the `content-frozen` baseline, verify protected sources, frozen tools and content write scope.
- [x] 1.3 Run the real G2 gate to enter `sources_frozen`; create only the empty fixed draft structure and ledger headers afterward.

## 2. Complete W0–W9 source review

- [x] 2.1 Fully read W0–W2; update inventory, continuous segments and atomic rules; run batch and scope checks.
- [x] 2.2 Fully read W3–W4; update inventory, continuous segments and atomic rules; run batch and scope checks.
- [x] 2.3 Fully read W5–W6; update inventory, continuous segments and atomic rules; run batch and scope checks.
- [x] 2.4 Fully read W7–W9; update inventory, continuous segments and atomic rules; run batch and scope checks.
- [x] 2.5 Run global inventory, segment and rule checks and complete G3 `sources_reviewed`.

## 3. Classify rules

- [x] 3.1 Assign every atomic rule one primary category/item, record secondary impacts, clusters, overlaps and conflicts.
- [x] 3.2 Build one conflict/ownership review packet and obtain required user decisions without auto-resolving them.
- [x] 3.3 Run classification and scope checks and complete G4 `rules_classified`.

## 4. Rewrite requirements

- [x] 4.1 Rewrite rules into the eleven fixed item documents with stable target IDs and no prohibited direct copying.
- [x] 4.2 Complete merge/split/supersede/retire mappings, preserve technical boundaries in execution details and disclose warnings.
- [x] 4.3 Run rewrite, draft, source and scope checks and complete G5 `rules_rewritten`.

## 5. Derive principles and complete structure

- [x] 5.1 Form rule-supported principles for all eleven items.
- [x] 5.2 Form cross-item principles for all four categories and complete category interfaces and continuous-evolution relationship.
- [x] 5.3 Obtain user semantic confirmation, run principle/draft checks and complete G6 `principles_derived`.

## 6. Audit and final review package

- [x] 6.1 Generate coverage matrix, conflicts, retirement candidates and deterministic content review report.
- [x] 6.2 Verify the report is current and complete G7 `audit_ready`.
- [x] 6.3 Run all applicable checks, source/scope protection and OpenSpec strict validation; complete producer self-check.
- [ ] 6.4 Complete G8 `awaiting_user_review`, stop all writes and deliver the full review package to the user.

## 7. User review boundary

- [ ] 7.1 Record only actual user decisions from the final review; do not claim independent review.
- [ ] 7.2 If explicitly approved, complete at most G9 `approved_for_migration_design`; do not migrate, delete, archive, commit or push.

## Execution Evidence

- G1 passed on 2026-07-16 with scope `761119503592150159985ff14f93d21950589af0e1858ab5c4819a73f7b0e962` after the user explicitly directed continuation of the overall rewrite.
- Change A tool manifest: `76c857c9187c03bf3b41e4e55f1cffd72862ac1824b31f7b8b78f0651082592f`; 62/62 tests and all six named fixture families passed.
- Protected W0–W9 source manifest before G2: `ac382ac1d71fae8e012b543b173e2c6eba7c1dfaabd4701c8d3633d5af5f653c`, 40 files.
- The legacy repository-wide review snapshot verifier remains a recorded known limitation and is not relabeled PASS.
- Change B pre-implementation strict validation exited 0 with 4/4 artifacts complete.
- G2 passed with the content-frozen baseline, 40 protected sources, 23 frozen tool files, 670 preserved workspace files and 669 preserved Git status entries.
- After G2 an operator invocation incorrectly used `scope-check --phase tooling`; the tool correctly blocked the already-authorized Change B paths. Root cause was the wrong phase argument, not a scope delta. No policy or baseline was relaxed; the subsequent content baseline and `--phase content` checks passed. A later out-of-scope edit to `governance/current-status.json` was restored byte-for-byte to the content baseline and content scope returned PASS.
- W0–W2 batch: 8/8 sources fully reviewed, 818 nonblank lines covered by 116 exact-once segments, and 835 atomic rules recorded. Initial task review found omitted field/path rules and over-merged collections; the batch was reworked from 509 to 835 traceable rules. Re-review concluded Spec compliance APPROVED and Task quality APPROVED with no remaining findings. Root rerun of baseline, content scope, inventory, segment and rule checks exited 0.
- W3–W4 batch: 13/13 sources fully reviewed, 1,525 nonblank lines covered by 202 exact-once segments, and 2,053 atomic rules recorded. Task review found omitted optional/exception semantics, over-merged checklists and OR rules that depended on neighbors; three correction/re-review rounds consolidated self-contained alternatives and removed logical duplicates. Final task review concluded Spec compliance APPROVED and Task quality APPROVED with no findings. Root rerun of all five batch commands exited 0.
- W5–W6 batch: 8/8 sources fully reviewed, 1,192 nonblank lines covered by 108 exact-once segments, and 1,330 atomic rules recorded. Task review found cross-segment line anchors, same-name schema mis-anchors, incomplete enum ranges and one strengthened OR state set; the batch was corrected and globally rescanned. Re-review concluded Spec compliance APPROVED and Task quality APPROVED with no findings. Root rerun of all five batch commands exited 0.
- W7–W9 batch: 11/11 sources fully reviewed, 1,607 nonblank lines covered by 147 exact-once segments, and 1,714 atomic rules recorded. Four review/correction rounds repaired nested schema conditions, W9 entry conditions, SRE templates, generic applicability, merged checklists, severity definitions/actions and conditional priority actions. Final task review concluded Spec compliance APPROVED and Task quality APPROVED with no findings. Root rerun of all five batch commands exited 0.
- Global G3 evidence: 40/40 inventory rows reviewed; 5,142 nonblank source lines covered exactly once by 573 segments; 5,932 atomic rules passed global validation. Baseline and content scope checks exited 0, and the real gate advanced to `sources_reviewed` / G3.
- Classification task evidence: seven correction/review rounds produced 5,956 uniquely classified atomic rules in 1,256 shared-decision clusters (maximum 36), with 161 rules carrying 171 secondary impacts, no confirmed semantic conflict and one disclosed ownership ambiguity (`RULE-W7-001-061`). Final task review concluded Spec compliance APPROVED and Task quality APPROVED with no findings. The ambiguity was subsequently resolved by the user's G4 approval.
- G4 passed on 2026-07-17 after the user explicitly approved the presented classification. `RULE-W7-001-061` remains assigned to `工程交付/验证`; the approval is recorded in `governance/rd-standards-rebuild/approvals.jsonl`, the ownership review packet has no remaining user decision, and the real gate advanced to `rules_classified`.
- G5 initiation batch: all 157 `选题` / `调研` rules were rewritten into 49 canonical targets (21 topic-selection, 28 research) with 141 `merge` and 16 `rewrite` treatments. After four correction/re-review cycles, task review concluded Spec compliance APPROVED and Task quality APPROVED with no findings. Root verification confirmed 157 mapped rows, 49 exact-once markers, no exact source copy, maximum similarity 0.885, protected sources and content scope unchanged, classification PASS and OpenSpec strict valid.
- G5 product-design batch: all 481 `定义` / `体验设计` rules were rewritten into 100 canonical targets (55 definition, 45 experience-design) with 462 `merge` and 19 `rewrite` treatments. Independent task review found and corrected OR-branch loss, unsupported strengthening, modal weakening, duplicate authority and copy-similarity issues; final Spec compliance and Task quality were APPROVED with no findings. Root verification confirmed 481 mapped rows, 100 exact-once markers, no exact source copy, maximum similarity 0.719, protected sources and content scope unchanged, classification PASS and OpenSpec strict valid.
- G5 technical-design batch: all 1,393 `技术设计` rules were rewritten into 816 canonical targets with 814 `rewrite`, 578 `merge` and one `split` treatment (`RULE-W2-093` maps to prompt-builder placement and validation/release handoff targets). Independent review required restoration of closed enums, paths, schema lists and human-review triggers, consolidation of two duplicate-authority pairs, and text corrections; after rework and re-review, Spec compliance and Task quality were APPROVED with no remaining findings. Root verification confirmed 1,394 target references, 816 exact-once markers, no missing or orphan target, no exact source copy, no similarity at or above 0.9 (maximum 0.891429), protected sources and content scope unchanged, classification PASS and OpenSpec strict valid.
- G5 planning/implementation batch: all 817 `计划` / `实现` rules were rewritten into 342 canonical targets (22 planning, 320 implementation) with 626 `merge` and 191 `rewrite` treatments. Independent review found and corrected three crossed W0/W5/W7 handoff mappings, one weakened icon-button conjunction, one changed AI-capability applicability condition, one unsupported prompt-storage strengthening and two stale merge-count note groups. Final Spec compliance and Task quality were APPROVED with no remaining findings. Root verification confirmed 817 target references, 342 exact-once markers, no missing, duplicate or orphan target, no exact source copy, no similarity at or above 0.9 (maximum 0.896552), all 626 merge-row notes consistent with the 151 actual merge-target source counts, protected sources and content scope unchanged, classification PASS and OpenSpec strict valid.
- During planning/implementation verification, an intermediate global `rewrite-check` was incorrectly invoked before the remaining `验证` / `发布` / `运行` / `评估` items were rewritten. It returned BLOCKED for those expected unreviewed rows; no state advanced and the result was not used as batch evidence. Subsequent verification used a read-only item-scoped checker, reserving the global command for completion of all eleven items.
- A later root-only merge-note diagnostic also returned nonzero because its first expression required the note to equal `合并 N 条同权威意图` and rejected the valid `G5 ...；` prefix. The diagnostic made no writes; the corrected extraction compared only the embedded count and passed for 626 merge rows across 151 merge targets with zero mismatches.
- G5 verification batch: all 1,096 `验证` rules were rewritten into 666 canonical targets with 529 `rewrite` and 567 `merge` treatments. Independent review found and corrected eight important groups covering exact navigation/artifact paths, W7 routing terminology, container exclusivity, closed evidence/risk trigger lists and runnable frontend commands, plus two minor terminology/readability groups. Final re-review by the new independent reviewer concluded Spec compliance APPROVED and Task quality APPROVED with no findings. Verification confirmed 1,096 target references, 666 exact-once markers, no missing, duplicate or orphan target, all 567 merge-row notes consistent across 137 merge targets, no exact source copy, and no similarity at or above 0.9 (maximum 0.896552); protected sources and content scope were unchanged, classification remained 5,956/5,956 PASS and OpenSpec strict validation passed.
- The verification batch retains its failed-attempt history: an earlier independent reviewer stopped after three nonzero read-only PowerShell review commands and issued no complete approval; a later correction executor initially stopped without writes when three referenced skill-package support documents were absent. Previously reported patch/query failures likewise did not advance state or supply approval evidence. Work resumed only in later bounded attempts, and the final approval above comes from the replacement independent reviewer after direct source comparison and regression checks.
- G5 release batch: all 467 `发布` rules were rewritten into 72 canonical targets with 480 target references and final treatments of 413 `merge`, 6 `rewrite`, 9 `split` and 39 `supersede`. Independent review required restoration of customer/claim root paths, recommendation modality, migration and pipeline responsibility mappings, upstream return paths, multi-intent gate splits, limited-trial human checkpoints, actual AI adversarial execution, executable stop conditions, claim responsibility separation, four-golden-signal coverage and low-risk release-file naming. Final bounded re-review concluded Spec compliance APPROVED and Task quality APPROVED with no findings. Root verification confirmed 72 exact-once markers, no missing, duplicate or orphan target, complete split notes, zero merge-note count mismatches across 413 merge rows and 61 merge targets, 467/467 valid source anchors, no blocking direct copy and no similarity at or above 0.9 (maximum 0.587859); the exact `runs/YYYY-MM-DD-<service>-<release-id>.md` path is retained as a non-rewritable technical code literal under the plan's section 18.4 exception. Protected sources and content scope were unchanged, classification remained 5,956/5,956 PASS and OpenSpec strict validation passed.
- The release batch also retains its failed-attempt history: one initial producer stopped without writes after searching for a non-existent alias instead of the authoritative `cluster_id`; one correction run reported a rejected atomic patch but incorrectly continued and was interrupted, after which a separate read-only audit froze and validated the resulting state; several reviewer attempts stopped without approval after nonzero optional or malformed read-only commands; one fix attempt stopped without writes after invoking unit tests instead of the formal baseline/scope commands; and one root audit wrapper failed before launching its read-only check. None of those failed attempts advanced state or supplied approval evidence. The final release evidence above comes from successful root verification and the independent reviewer that had completed the original full semantic review and then approved both bounded correction rounds against frozen revisions.
- G5 operation batch: all 971 `运行` rules were rewritten into 144 canonical targets with 956 `merge` and 15 `rewrite` treatments. Independent review completed full semantic coverage across 24 source files and found one blocking near-copy in the AI tool runtime logging rule: the nine-field audit contract and prompt/response redaction boundary were retained, but the mandatory/prohibitive prose was restructured from a 0.985 near-copy to approximately 0.182 similarity. Final bounded re-review concluded Spec compliance APPROVED and Task quality APPROVED with no findings. Verification confirmed 971 exact target references, 144 exact-once markers, no missing, duplicate, orphan or empty target, correct merge counts and preserved `原注=` context, 971/971 valid source anchors, and no unexpected similarity at or above 0.9. The remaining 11 high-similarity rows are the exact `credentials[].type` closed enum values, and four exact field-list matches are AI telemetry technical fields; both were independently confirmed as non-rewritable technical literals under section 18.4. Protected sources and content scope were unchanged, classification remained 5,956/5,956 PASS and OpenSpec strict validation passed.
- The operation batch retains its incomplete-attempt history: the producer disclosed one non-authoritative self-check warning caused by a stale global row-count assumption, while the authoritative classification check passed 5,956/5,956; the first independent mechanical pass stopped without approval after querying `source-inventory.csv` with a nonexistent `source_path` column. A fresh bounded pass used the authoritative atomic-ledger columns, completed all 971 source anchors and mechanical checks, and the final approval above was issued only after the near-copy correction and frozen-revision re-review.
- G5 evaluation batch: all 574 `评估` rules were rewritten into 124 canonical targets with 541 `merge` and 33 `rewrite` treatments. Independent review found and corrected the administration repetition threshold, W9 new-work exit routing, open-list and modality preservation, cross-item authority fragmentation for W8 exits, context packs, context recovery and AI upgrade evaluation, and unsupported cadence or obligation additions. Final bounded re-review concluded Spec compliance APPROVED and Task quality APPROVED with no findings. Root verification confirmed 574 exact target references, 124 exact-once markers, no missing, duplicate, orphan or empty target, 574/574 valid source anchors, no similarity at or above 0.9 (maximum 0.887781), protected sources and content scope unchanged, classification remained 5,956/5,956 PASS and OpenSpec strict validation passed. The only direct-copy candidates are the exact canonical paths `governance/knowledge/docs-map/<target>.json` and `governance/knowledge/context-packs/<target>.md`, retained as non-rewritable technical literals under section 18.4.
- The evaluation batch retains its incomplete-attempt history: the first reviewer attempt stopped without approval after using an incorrect release-document path; the first correction attempt stopped without writes when its patch context no longer matched; and one root read-only wrapper failed before launching because of a JavaScript quoting error. None advanced state or supplied approval evidence. The final evidence above comes from successful root verification and a separate independent reviewer after the bounded corrections.
- G5 global convergence: the first complete `rewrite-check` exposed 10 exact-copy and 25 high-similarity findings. Ten bounded prose edits preserved the affected release path, AI telemetry fields, credential enum, knowledge paths, recovery order and minimal-kernel precedence while removing contiguous source prose; an additional structure check found and corrected the missing `按主题整理的执行细则` boundary in the operation item. A stricter punctuation-normalized audit then found four further complete-copy candidates, and a treatment audit found two release claim rows marked `merge` despite single-source targets. The four passages were rewritten without dropping their lists, routes, evidence fields or safety prohibition; `RULE-W6-004-105` and `RULE-W6-004-145` were corrected to `rewrite`. Final treatments are 4,282 `merge`, 1,625 `rewrite`, 39 `supersede` and 10 `split`; the release item is 411 `merge`, 8 `rewrite`, 39 `supersede` and 9 `split`.
- G5 global independent semantic review initially rejected `RELEASE-CLAIM-008` and `RELEASE-CLAIM-009` because their single-source rewrites added evidence-version, review-schema and old-approval obligations and competed with evaluation authority. The targets were narrowed to the source-owned evidence-link and paid-customer cadence duties, with the review record contract delegated to `EVALUATION-CLAIM-001`. A bounded re-review caught and corrected one remaining narrowing of the unconditional every-release cadence. At `09-release.md` SHA-256 `6148E607C22FD4291BD53BB75D7EB02B5813B0F46F5F4A6ED42B8B36A48BBAEC` and `atomic-rules.csv` SHA-256 `C65518CD111EBBFFB42665C1CC49618ABD299F3FAD4A701ADD93733637387FE4`, the independent reviewer concluded Spec compliance APPROVED and Task quality APPROVED with no Critical, Important or Minor findings. Existing item-level independent reviews remain the full semantic evidence for their item revisions; the global reviewer covered cross-item authority and the bounded corrections.
- G5 global verification on the final revision passed protected-source baseline (40 files), content scope (670 preserved workspace files and 669 preserved Git status entries), inventory (40/40), continuous segments (5,142 nonblank lines / 573 segments), atomic rules and classification (5,956/5,956), draft structure (4 categories / 11 items / 2,313 canonical targets), and rewrite traceability (5,956 rules / 2,313 markers / 0 exact-copy / 0 high-similarity review / 0 warnings). The supplemental punctuation-normalized copy count and singleton-merge count are both zero, and OpenSpec strict validation passed.
- The global closeout retains its incomplete-attempt history: several replacement reviewers stopped before issuing a verdict after missing-file, parser or quoting errors in optional read-only commands; one root plan-search likewise returned nonzero because PowerShell interpreted backticks. Earlier long-command output capture also created redundant read-only scans before they were cleaned up. None of these attempts wrote semantic content, advanced a gate or supplied approval evidence. Only the successful final formal checks and the independent semantic review described above are used as G5 evidence.
- G5 passed on 2026-07-18 when the real `gate --target rules_rewritten` reran the authoritative rewrite check and exited 0. `governance/rd-standards-rebuild/run-state.json` advanced from `rules_classified` to `rules_rewritten` with scope SHA-256 `761119503592150159985ff14f93d21950589af0e1858ab5c4819a73f7b0e962`; no human approval was claimed or required for this gate.
- G6 principle derivation initially produced 30 candidates, but the first incomplete reviewer attempt exposed overgeneralization and was not used as approval evidence. The principles were rederived into 15 minimal candidates: one for each of the eleven items and one for each of the four categories.
- A new independent reviewer completed the full G6 semantic review and rejected the first 15-principle revision with six Important findings: missing topic-selection risk support, incomplete research exit support and wording, narrowed category decision guidance, missing technical-design API/error-model support, missing evaluation signal support, and weakened/expanded operation SLA conditions. All six groups were corrected against the atomic-rule and cluster ledgers.
- The same independent reviewer completed a bounded closing review of the corrected revision and concluded Spec compliance APPROVED and Task quality APPROVED with no Critical, Important or Minor findings. The final trace contains 15 candidate principles supported by 25 unique atomic rules and 19 clusters; 107 cluster members were covered by the review, all principle statements match their draft documents, and rule-to-cluster references are bidirectionally consistent.
- Immediately before approval, the final pre-approval checks passed for protected sources, content scope, rewrite traceability (5,956 rules, 2,313 canonical targets, zero review-required findings and zero warnings), principle structure (11 item and four category principles), draft structure (11 items and four categories) and OpenSpec strict validation. At that checkpoint, user semantic approval was still pending, all 15 principles remained `candidate`, task 5.3 was open and no `principles_derived` gate had been run.
- On 2026-07-18 the user explicitly responded `批准` after the approval scope was stated as the fifteen G6 item/category principles and their semantic structure, excluding final migration. The decision is recorded as `G6-2026-07-18-user-approved-principles`; all 15 principle rows were changed from `candidate` to `approved` without changing their statements or support links.
- G6 passed on 2026-07-18 after protected-source and content-scope checks, the 15-principle traceability check, the four-category/eleven-item draft check and OpenSpec strict validation all exited 0. The real `gate --target principles_derived` advanced `run-state.json` from `rules_rewritten` to `principles_derived`; no G7 or later result is claimed by this transition.
- G7 report generation first passed a no-write dry-run and then created the four plan-defined outputs: the 5,956-row coverage matrix, conflict list, retirement-candidate list and deterministic content review report. The report records 40 sources, 573 segments, 5,956 atomic rules, 15 approved principles, zero unresolved conflicts and zero retirement candidates, and remains explicitly marked `semantic_correctness: not-assessed` / `user_review: required`.
- Producer audit matched all 5,956 unique coverage rows to the atomic ledger with zero ID or field deltas, zero retained rules without targets, all 40 inventory paths covered, and all 15 principles represented by valid support rows. An initial optional PowerShell distribution query exited nonzero because of invalid `Sort-Object` syntax and made no writes; the turn stopped as required. After fresh status, baseline and scope checks, the corrected read-only query exited 0 and is the only distribution result used below.
- The user-facing draft entry now records the representative lookup routes, old W0-W9 to new-item review mapping, default three-layer navigation, exact item reading volume and consolidation facts. The eleven item documents contain 5,061 nonblank lines and 2,313 canonical targets; 4,282 merge rows converge on 833 shared targets, reducing 3,449 repeated authority expressions without treating that reduction as semantic deletion. The report was regenerated after these entry changes, and the G7 gate dry-run passed for the current inputs; task 6.2 and the real gate remain pending until the next recorded transition.
- G7 passed on 2026-07-18 when the real `gate --target audit_ready` verified that the four audit outputs exist and that `content-review-report.json` matches the current ledger and draft inputs. `run-state.json` advanced from `principles_derived` to `audit_ready`; no G8 result or final user acceptance is claimed.
- G8 producer verification ran `verify-all` on the G7 revision and all eleven applicable checks passed: baseline, content scope, required files, inventory, segments, atomic rules, classification, rewrite/copy, principles, draft and current report. The authoritative totals remain 40 sources, 5,142 covered nonblank source lines in 573 segments, 5,956 classified and rewritten rules, 2,313 exact-once target markers, 15 approved traceable principles and four current audit outputs, with zero review-required rewrite findings or warnings.
- OpenSpec strict validation exited 0 and `openspec status --change rewrite-rd-standards-content` reported all four artifacts complete. A separate read-only producer completion audit passed for all 16 fixed draft documents, eight review-package outputs, G4/G6 user approval records, exact treatment totals (`merge` 4,282; `rewrite` 1,625; `split` 10; `supersede` 39), user-review boundary and `audit_ready` state. Task 6.4, the independent final review and the real G8 gate remain pending.
- The first fresh G8 review attempt supplied no approval: its auxiliary read-only PowerShell distribution query exited nonzero, so the reviewer stopped as required. Before stopping it identified two substantive usability defects for producer verification: `rebuild-draft/README.md` copied a dynamic gate state, and new item prose still treated old W-stage material as an operational authority. This incomplete attempt is retained as rejection/correction history and is not independent approval evidence.
- Producer verification confirmed both findings against the current workspace. The root entry now links `governance/rd-standards-rebuild/run-state.json` as the sole dynamic state fact instead of copying a state. After the user explicitly approved contextual route correction scheme B, five item documents were corrected so user-visible behavior routes to definition/experience design, AI architecture/model/tool/RAG/memory and eval design route to technical design, code changes route to implementation, proof routes to verification, production facts route to operation, and learning or maintenance decisions route to evaluation. Stable rule IDs, HTML markers and review-ledger provenance were preserved.
- The correction regression scan found zero standalone old-stage labels and zero direct `docs/W0-*` through `docs/W9-*` paths in all four category and eleven item documents; the root entry retains W labels only in its explicitly historical/source mapping. All 2,313 canonical rule markers remain unique, and all 92 local Markdown routes in the five corrected item documents resolve to existing files.
- Post-correction producer checks exited 0 for protected-source baseline, content scope, rewrite traceability/copy detection, principle traceability and draft structure. `report-build --dry-run` and the real `report-build` both passed; the four G7 outputs were regenerated with current input digest `6f4e12f2061f67bc09439578dbda76cdf12548577176de46febaa8cd87419dc0`. The subsequent `verify-all` reran and passed all eleven applicable checks with 40 protected sources, 5,142 covered nonblank lines, 573 segments, 5,956 classified/rewritten rules, 2,313 exact-once targets, 15 approved principles, zero rewrite review findings and zero warnings. A new independent G8 review and the real G8 gate remain pending.
- The first post-correction replacement reviewer also supplied no approval. It independently ran all six required formal read-only commands with exit 0 and confirmed G7, the protected source/tool manifests, all eleven `verify-all` checks, current report digest and 4/4 OpenSpec artifacts. Its subsequent auxiliary inventory command returned exit 1 because of a PowerShell pipeline syntax error, so it stopped immediately with `INCOMPLETE/REJECTED` before completing the 16-document semantic review and ledger/governance cross-check. It made no writes, did not run a gate, and is retained only as incomplete review history; task 6.4 remains open.
- A later clean-review attempt stopped before examining any workspace deliverable because the applicable `one-person-openspec-rd` skill package references `C:/Users/machinly/.codex/skills/one-person-openspec-rd/README.md`, which is absent; its literal read returned exit 1. The reviewer made no writes, ran none of the six formal commands and returned `INCOMPLETE/REJECTED`, so it is not approval evidence. Root-cause verification subsequently confirmed the package file is absent and that this is an external skill-package defect, not a content or gate failure. The next fresh reviewer must use the skill's documented missing-resource fallback and the authoritative content-rewrite plan rather than attempting the known-missing path.
- Fresh independent G8 reviewer `/root/review_g8_final_4` completed the full 16-document draft read, representative ledger/governance cross-check and all six required formal read-only commands against report input digest `6f4e12f2061f67bc09439578dbda76cdf12548577176de46febaa8cd87419dc0`. The review found no Critical issue but returned `REJECTED` with three Important and one Minor finding: the root entry repeated a principle-approval fact instead of consistently routing authoritative state and approval evidence; several rules still treated the old maintenance project, `00-main.md` hierarchy or `verify_workflow_index.py` as current operational authority; AI work routing still mixed user-visible behavior, interaction design, technical design, implementation and proof; and one release trace line still described a structure placeholder. This is valid rejection/correction evidence, not G8 approval.
- Producer evaluation confirmed the actionable findings and corrected only six bounded draft files. `rebuild-draft/README.md` now links principle state, actual approval records and the live gate state to their single authoritative files. Planning, implementation, verification, release and evaluation rules now route definition, experience, technical design, implementation, verification, release, operation and evaluation responsibilities to the corresponding new item documents; the obsolete maintenance-project/current-`00-main.md`/`verify_workflow_index.py` authority model was replaced by the four-category/eleven-item draft and `rd_rebuild.py draft-check`; the release placeholder was replaced by static rule-ID and coverage-matrix trace guidance. Stable rule IDs, target markers and ledger provenance were preserved.
- Bounded post-fix diagnostics found zero stale old-route lines, zero stale authority lines, zero non-root old W-stage routes, 2,313 unique rule markers with no duplicate, and 127/127 resolving local links across the six corrected files. Their SHA-256 revisions are `README.md` `73EBA689EDEF461C79CB8629E49C9D837FF449AC7096ADEC2A6E914066859803`, planning `8C720D0F0B97680AB7804B5B8FF871D1FEACAA4D3411244E62AE367D208C77BC`, implementation `26CD79C8236F4841DD5938C3A96FF7976AD573CC6B3698D8EE0C1F313DB6C695`, verification `EEC5829A12193DA6802CDAF4AD54550CD3EE5254B272E67818A6E84E074C1067`, release `916BDC0D7BFE479A0E96F0BA5B53BD393FFBFBDF68EAC5ADE122E673E383A970`, and evaluation `20D1472A0ECD975F0777AFAFEEDF6F243685421A0808D3EA691C03A09BB19DD7`.
- The corrected revision passed `draft-check`, `rewrite-check`, `principle-check`, protected-source baseline and content scope. Report dry-run and real generation both passed with current input digest `f9be09b00a82453b22994093bd33c8871a03aafbb46eeeece3037c9762933e59`; the generated JSON report SHA-256 is `078C384F70DB9C1F3381CC23C32BE978112958BC5FA2D29FDDF5B1F6E75A8E76`. A subsequent `verify-all` passed all eleven applicable checks with the authoritative totals of 40 sources, 5,142 nonblank lines in 573 segments, 5,956 classified/rewritten rules, 2,313 exact-once targets, 15 approved principles, zero review-required findings and zero warnings. OpenSpec strict validation exited 0, protected sources and frozen tools retained their manifests, content scope remained unchanged, and the run state remained G7 `audit_ready`. A brand-new independent closing review and the real G8 gate remain pending; task 6.4 is intentionally unchecked.
- Independent closing reviewer `/root/review_g8_closing_fresh` fully read the six bounded correction files and verified all six supplied SHA-256 revisions. Before the mandatory stop, it found the README authority links, eleven-item routing, AI responsibility split, replacement of the old maintenance/`00-main.md`/`verify_workflow_index.py` model and release trace wording directionally aligned with the four prior finding groups. The review then returned `INCOMPLETE/REJECTED` when a direct read of `rebuild-draft/review/classifications.csv` exited 1. Root-cause investigation confirmed this filename, and the later-requested `rewrite-map.csv`, were erroneous additions in the reviewer instruction: neither is a plan-defined artifact. The authoritative classification, treatment and target fields are in `atomic-rules.csv`, with final mapping evidence in `coverage-matrix.csv`; the seven actual review-package files exactly match section 4 of the execution plan. The reviewer stopped before governance checks and the six formal commands, made no writes and ran no gate, so this attempt supplies no G8 approval. Task 6.4 remains open pending a new reviewer using only plan-defined files.
- Fresh closing reviewer `/root/review_g8_closing_corrected` fully read and hash-verified the six correction files, checked all thirteen named target IDs against `atomic-rules.csv` and `coverage-matrix.csv`, confirmed the governance/report digests and G7 boundary, and ran all six formal read-only commands with exit 0. It confirmed closure of the README single-authority issue, old maintenance/`00-main.md`/`verify_workflow_index.py` routing, and release placeholder wording, but found one remaining Important issue at `VERIFY-EVIDENCE-RISK-W5-001-L011`: an unqualified AI-behavior change still routed only to technical design instead of separating product behavior, interaction and technical scheme responsibilities. A supplementary exact-ledger read later exited 1 with no output, so the reviewer correctly returned `INCOMPLETE/REJECTED` and supplied no approval. It made no writes and ran no gate.
- Producer source comparison confirmed the Important finding against `RULE-W5-001-007` and the old W3-wide return boundary. A bounded zero-exit diagnostic first reported `RED` because the target contained none of the new definition, experience-design and technical-design links. Only the target statement in `08-verification.md` was changed: user-visible AI behavior or input now returns to definition, interaction and perception return to experience design, and prompt/eval/model/routing/RAG/tool/memory schemes return to technical design; the rule ID, its three atomic mappings and coverage rows remain unchanged. The identical diagnostic then reported `GREEN` with all three routes present. The new verification-document SHA-256 is `B8F71A857C7B8439685B1AF78A2A38F5109274E7C04004527493E7B7D6B60485`; the other five correction-file hashes remain unchanged.
- After the bounded fix, protected-source baseline and content scope passed; `draft-check` retained four categories, eleven items and 2,313 targets; `principle-check` retained fifteen approved traceable principles; and `rewrite-check` retained 5,956 rules, 2,313 target markers, zero review-required findings and zero warnings. Report dry-run and real generation passed with input digest `1e9c9ab4aa6796748778b5e0858ca7f33856f1497819f63db15be95985b84136`; the current report JSON SHA-256 is `5158AF5DE0F6418F83C8E1A7462A77E56D38B46A4DD25C7D01115BFE22D773D9`. A fresh `verify-all` passed all eleven applicable checks, OpenSpec strict validation exited 0 and status remained 4/4 artifacts complete. G8 remains unclaimed and task 6.4 intentionally remains unchecked pending a new independent closing review of this revision.
- Brand-new independent reviewer `/root/review_g8_final_closure` completed the final read-only closing review on 2026-07-19. It fully read and hash-verified the six frozen correction files, independently checked closure groups A–E, found all fourteen named targets in both `atomic-rules.csv` and `coverage-matrix.csv`, confirmed the three unchanged atomic/coverage rows for `VERIFY-EVIDENCE-RISK-W5-001-L011`, and verified the G7 governance boundary, current report digest and zero-conflict/zero-retirement state. The reviewer found zero Critical, Important or Minor issues and returned `APPROVED / accept`.
- The final reviewer ran the six required commands as its last six tool actions: `status`, `baseline-verify`, content `scope-check`, `verify-all`, OpenSpec strict validation and OpenSpec status all exited 0; `verify-all` passed all eleven applicable checks and OpenSpec reported 4/4 artifacts complete. It made no writes and ran no gate, report build or Git action. Browser E2E and template provenance were explicitly assessed as not applicable to this local documentation-only rewrite. Residual boundaries remain: the report still correctly states `semantic_correctness: not-assessed` / `user_review: required`, and this approval authorizes only entry to G8 user review, not G9, migration, formal-entry replacement, archive or deletion.
- Immediately after receiving the independent approval, producer `status`, protected-source baseline and content scope checks exited 0 and confirmed the unchanged G7 target revision. Task 6.4 remains unchecked before the real G8 transition. If the G8 gate succeeds, the plan requires all workspace writes to stop immediately, so no post-G8 task-file edit will be made merely to change this checkbox; the authoritative completion evidence will be `run-state.json` at `awaiting_user_review` / G8 and the delivered review package.
- G8 dry-run did not advance. `gate --target awaiting_user_review --dry-run` returned exit 1 with `STATUS=TOOL_ERROR` and `openspec-strict: TOOL_ERROR`; `run-state.json` remained `audit_ready` / G7 and the dry-run made no writes. The failure is recorded in `governance/rd-standards-rebuild/reports/2026-07-19-g8-openspec-launcher-tool-error.json` and is not relabeled as a content, OpenSpec or independent-review failure.
- Root-cause evidence shows that standalone `openspec validate rewrite-rd-standards-content --strict --no-interactive` continues to exit 0, while the frozen gate's shell-free `subprocess.run(["openspec", ...])` returns `[WinError 2]` on this Windows machine. PowerShell resolves `openspec.ps1`, Python discovery resolves `openspec.CMD`, and the existing gate tests contain no OpenSpec process-launch case. No frozen tool or manifest has been changed. Under the plan, recovery requires returning to Change A for a regression-tested cross-platform launcher fix and renewed G1 evidence; because the plan does not define how a post-G2 tool digest is rebound to the current content-frozen baseline, a narrow user-approved recovery/refreeze protocol is required before any such write. Task 6.4 remains stopped and unchecked.
